<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:map="clr-namespace:i4prj.SmartCab.CustomControls"
             x:Class="i4prj.SmartCab.Views.RideConfirmationPage"
             Title="Bekræft tur">
    <ScrollView Orientation="Vertical" InputTransparent="true">
        <StackLayout VerticalOptions="StartAndExpand">

            <map:BindableMap WidthRequest="100" HeightRequest="250"  MapPosition="{Binding PositionOfMap}" MapPins="{Binding LocationPins}" MapType="Street" Radius="{Binding MapRadius}"></map:BindableMap>

            <StackLayout Orientation="Vertical" Margin="10">
                <Label FontAttributes="Bold" FontSize="Large" Text="{Binding Price}"></Label>
                <Button Text="Bekræft" Command="{Binding ConfirmCommand}" IsVisible="{Binding IsReady}"></Button>
                <ActivityIndicator IsRunning="true" IsVisible="{Binding IsBusy}" />
            </StackLayout>

        </StackLayout>
    </ScrollView>
</ContentPage>